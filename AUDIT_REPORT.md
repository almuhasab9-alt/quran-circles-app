# 📋 تقرير الفحص الفني الشامل — تطبيق متابعة حلقات مركز السنة
**المشروع**: `almuhasab9-alt/quran-circles-app` (إصدار 1.0.0+1)
**تاريخ الفحص**: 27 أغسطس 2026
**الممتحن**: مراجعة كود + تحليل ثابت + اختبارات آلية + فحص أحي للخوادم + محاولة بناء APK

---

## 🎯 الخلاصة التنفيذية

التطبيق مبني جيداً من حيث البنية المعمارية (طبقات واضحة، واجهات مستودعات، Riverpod) وجودة الكود عالية، لكن **النسخة الحالية غير جاهزة للاستخدام الفعلي** للأسباب التالية:

| الخطورة | العدد | الأبرز |
|---|---|---|
| 🔴 حرجة (توقف كامل/اختراق) | 3 | تزوير توكنات المشرف، حسابات المعلمين لا ترى شيئاً، السجلات اليومية لا تصل السحابة |
| 🟠 عالية | 5 | توقيع الـ APK بمفتاح تجريبي، غياب التحكم بالصلاحيات في البيانات |
| 🟡 متوسطة | 7 | اقتطاع 500 سجل، تسجيل دخول لا يُحفظ، ثغرات تشغيلية |
| 🔵 منخفضة/شكلية | 5 | توثيق قديم، lint، CI قديم |

**الحكم**: ⚠️ لا تُوزَّع نسخة الـ APK الحالية على المعلمين قبل إصلاح البنود الحرجة الثلاثة.

---

## ✅ حالة الإصلاح (تحديث 27 أغسطس 2026)

**تم تنفيذ المرحلة الأولى كاملة والتحقق منها عملياً:**

| الإصلاح | الحالة | التحقق |
|---|---|---|
| 1. تدوير المفتاح السري وإزالته من الكود | ✅ منشور | التوكن المزور بالسر القديم → 401، والسر الجديد → 200 |
| 2. ربط حسابات الدخول بجدول المستخدمين | ✅ منشور + ترحيل البيانات | 16/16 حساباً لها مستخدم مقابل؛ 14/15 معلماً يرون حلقاتهم (المتبقي غير مسند أصلاً) |
| 3. رفع السجلات اليومية للسحابة | ✅ منشور (الخادم) + كود التطبيق جاهز | اختبار إدراج→تعديل لنفس اليوم نجح بسجل واحد محدَّث |

**ملاحظات تشغيلية**:
- بعد تدوير السر، تُرفض توكنات الدخول القديمة — سيُطلب من الجميع تسجيل الدخول من جديد (مرة واحدة).
- `@sim.teacher15` بلا حلقة مسندة — يسند المشرف له حلقة من شاشة «الحلقات ← تعديل».
- بناء الـ APK الجديد يتطلب تشغيل workflow الـ «Build Android APK» من صفحة Actions (أو سيتم مع أي إصدار)، وسيوقَّع للأسف بمفتاح التصحيح حتى تنفيذ إصلاح البند 4.

**ما زال مطلوباً (المرحلة 2)**: مفتاح توقيع إصدار للـ APK، فحوص صلاحيات الكتابة في الـ API، حفظ الجلسة محلياً، نقطة إحصاءات للوحة التحكم.

### ✅ تم تنفيذ المرحلة الثانية (27 أغسطس 2026)

| الإصلاح | الحالة | التحقق |
|---|---|---|
| فحوص صلاحيات الكتابة في الـ API | ✅ منشور | إدارة الكيانات للمشرف فقط؛ محاولة معلم إنشاء حساب مشرف → 403؛ كتابة السجلات اليومية ما زالت متاحة للمعلمين |
| حفظ الجلسة محلياً | ✅ في الكود (يشملها أي بناء جديد) | استعادة تلقائية + تحقق خلفي عبر `/api/me` + مسح عند الخروج أو الرفض |
| مفتاح توقيع إصدار للـ APK | ✅ منشور ومفعل في الـ CI | الحزم الجديدة موقعة بشهادة `CN=Quran Center Ataq` (SHA-256: `3E:AD:40:31:…:C9:2F`) صالحة حتى 2054 — بدل شهادة «Android Debug» |

**تنبيهات المرحلة 2**:
- مفتاح التوقيع (ملف `quran-center-release.p12`) محفوظ لدى المالك + نسخة مشفرة في أسرار GitHub. **فقدانه تعني عدم القدرة على تحديث التطبيق** — احتفظوا بنسخة خارجية آمنة.
- لأن التوقيع تغيّر، التحديث من نسخة «التوقيع التجريبي» القديمة يتطلب إلغاء التثبيت مرة واحدة.

### ✅ تم تنفيذ البند الأخير: نقطة إحصاءات للوحة التحكم (27 أغسطس 2026)

| الإصلاح | الحالة | التحقق |
|---|---|---|
| تجاوز حد الـ 500 سجل في لوحة التحكم | ✅ منشور على الخادم + كود التطبيق جاهز | نقطة `GET /api/stats` تجمع كل السجلات في قاعدة البيانات (25,576+ سجلاً) عبر SQL مجمّع في الخادم — استجابة واحدة بدل تحميل آلاف السجلات على الجهاز |

**التفاصيل**:
- الإحصاءات المجمعة: سجلات اليوم، إجمالي صفحات الحفظ الجديد، عدد طلاب «إعادة»، توزيع التقديرات (فطيرة)، صفحات الجديد لآخر 12 أسبوعاً (أعمدة)، مقارنة الحلقات، والطلاب الذين يحتاجون متابعة (إعادة ≥ 40% في ≥ 4 سجلات).
- النطاق حسب الدور: المعلم يرى إحصاءات حلقاته فقط (تُمرَّر معرّفاتها للخادم)، والمشرف يرى الكل.
- استُبدلت لوحة التحكم بالكامل لتستهلك الإحصاءات المجمعة؛ لم تعد تسحب قائمة السجلات الكاملة (التي كانت تُقتطع عند 500).

**اكتملت الآن خطة الإصلاح الثلاثية ببنودها جميعاً.** ✅

---

## ✅ ما تم اختباره فعلياً (وليس نظرياً)

| الاختبار | النتيجة |
|---|---|
| `flutter analyze` (Flutter 3.47.1 / Dart 3.13.1) | ✅ 0 أخطاء، 0 تحذيرات، 7 ملاحظات شكلية فقط |
| `flutter test` — 42 اختباراً | ✅ كلها ناجحة (بيانات المصحف، الأسابيع، التقديرات، PDF) |
| فحص أحي لـ `quran-circles-api` و `quran-auth-api` و `quran-center-bridge` | ✅ تعمل، والمصادقة تمنع الوصول بدون توكن |
| اختبار اختراق عملي (تزوير توكن بالمفتاح المسرّب) | ❌ **نجح الاختراق** — تفاصيله أدناه |
| اختبار تكامل المعرفات بين قواعد البيانات | ❌ 15 من 16 حساب معلم غير مرتبطة بأي مستخدم في البيانات |
| اختبار حدود البيانات | ❌ الاقتطاع عند 500 سجل مؤكد |
| فحص الـ APK الرسمي (نزلته من مخرجات GitHub Actions) | ❌ موقَّع بشهادة «Android Debug» |
| فحص محتويات الـ APK (الخطوط/الأصول) | ⚠️ خطوط عربية مضمّنة لكن غير مسجلة، وخط الواجهة يُحمَّل من الإنترنت |
| بناء APK محلي | ⏳ انظر الملحق (البيئة محدودة بـ 1.9GB) |

---

## 🔴 النتائج الحرجة (P0)

### 1. المفتاح السري لتوقيع التوكنات منشور في الكود — يمكن لأي شخص تزوير صلاحيات مشرف

**الدليل**: المفتاح `qc-auth-2a9f7e1c-secret-hmac-key-v1` موجود في المستودع **العام** في ثلاثة مواضع:
- `worker-auth/src/index.js` (سطر `const SECRET = ...`)
- `worker-auth/wrangler.jsonc` (`"vars": { "HMAC_SECRET": ... }`)
- `worker/src/index.js` (`const HMAC_SECRET = ...`)

**الإثبات العملي** (نفّذته فعلياً أثناء الفحص):
```
1. أنشأت توكن {sub: "security-audit", role: "supervisor"} ووقّعته بالمفتاح المنشور
2. طلبت /api/halaqas  → 200 ✅ (كل الحلقات)
3. طلبت /api/students → 200 ✅ (296 طالباً ببياناتهم)
4. طلبت /api/users    → 200 ✅
5. طلبت /auth/api/data → 200 ✅ (لقطة البيانات الكاملة)
```
أي شخص يطّلع على المستودع العام يستطيع تزوير توكن مشرف والوصول لكل البيانات، **بل وتمرير `role: supervisor` لتنفيذ `/api/wipe` وحذف جميع البيانات**.

**الإصلاح العاجل**:
1. تدوير المفتاح فوراً: `wrangler secret put HMAC_SECRET` (لكلا الـ Workerين) وحذفه من `vars` في `wrangler.jsonc` ومن الكود المصدري.
2. اعتبار كل البيانات مكشوفة سابقاً.
3. نقل المستودع إلى خاص (أو تنظيف التاريخ بـ `git filter-repo`، فالمفتاح موجود في تاريخ الـ commits أيضاً).

### 2. حسابات المعلمين الحقيقية لا ترى أي بيانات في التطبيق

التطبيق يستخدم **نظامي مستخدمين منفصلين لا يتقاطعان**:
- قاعدة المصادقة (`quran-auth-db`): جدول `accounts` — فيه حسابات الدخول الفعلية.
- قاعدة البيانات (`quran-circles-db`): جدول `users` — فيه المستخدمون المزروعون تجريبياً فقط، وإليهم تُسنَد الحلقات عبر `teacher_ids`.

**الإثبات العملي**:
```
حسابات الدخول الحقيقية: 16
المستخدمون في قاعدة البيانات: 18
المعرفات المشتركة: 1 فقط (حساب المشرف @admin)
→ 15 حساب معلم: ❌ غير موجودة في جدول users
→ معرفات المعلمين المعيّنة في الحلقات (14): لا يطابق أيٌّ منها حساب دخول حقيقي
```

**الأثر على المعلم في الـ APK**:
- لوحة التحكم تفلتر الحلقات بـ `h.teacherIds.contains(session.userId)` → **قائمة فارغة**.
- شاشة الحلقات: "لا توجد حلقات".
- النتيجة: المعلم يدخل بحسابه الصحيح ويرى تطبيقاً فارغاً تماماً.

**الإصلاح**: عند إنشاء حساب معلم في `/api/accounts` يجب إنشاء/ربط مستخدم مقابل في `users` بنفس المعرّف، أو ترحيل المعرفات بحيث يتطابق النظامان، وإتاحة إسناد الحلقات لحسابات الدخول من شاشة المشرف.

### 3. السجلات اليومية التي يدخلها المعلم تُحفظ محلياً فقط ولا تصل السحابة

`DailyEntryScreen._save()` يستدعي `SessionService` الذي يكتب في قاعدة **Drift المحلية** على الجهاز:
```
lib/features/teacher/daily_entry_screen.dart:75 → svc.saveDailyRecord(...)  (محلي)
```
بينما كل الشاشات تقرأ من السحابة (`RemoteStudentRepository`, `allRecordsProvider`...). ومستودع السجلات البعيد `RemoteDailyRecordRepository` **للقراءة فقط** — لا توجد دالة كتابة فيه أصلاً.

**الأثر**:
- المشرف لا يرى أي سجل يدخله المعلم.
- سجلات المعلم تضيع بإعادة التثبيت أو تغيير الجهاز.
- كشف التسميع الأسبوعي (`WeeklySheetScreen`) والتقارير (`ReportService`) تقرأ من المحلية أيضاً → تظهر فارغة رغم وجود بيانات سحابية.
- النسخ الاحتياطي يصدّر قاعدة محلية شبه فارغة.

**الإصلاح**: تحويل `SessionService`/`ReportService` للعمل عبر `recordRepoProvider` السحابي + إضافة مسارات `POST/PUT` للسجلات في الـ Worker (انظر البند 7).

---

## 🟠 النتائج العالية (P1)

### 4. الـ APK موقَّع بمفتاح تصحيح (debug key) يتغير مع كل بناء
`android/app/build.gradle.kts`:
```kotlin
release { signingConfig = signingConfigs.getByName("debug") }
```
**فحصت الـ APK الفعلي من مخرجات CI (`app-arm64-v8a-release.apk`)**:
```
Signer #1 certificate DN: C=US, O=Android, CN=Android Debug  ← شهادة تصحيح!
```
- كل بناء من GitHub Actions ينشئ مفتاح تصحيح جديداً → توقيع مختلف كل مرة → **تحديث التطبيق يتطلب إلغاء التثبيت** (فقدان البيانات المحلية).
- لا يمكن نشره على المتاجر، وبعض الأجهزة تقيّد تثبيت التحديثات الموقّعة بهذا النوع.
- بقية بيانات الحزمة سليمة: `minSdk 24`، `targetSdk 36`، الأذونات `INTERNET` فقط ✅.
**الإصلاح**: إنشاء مفتاح إصدار ثابت (`keytool`) وحفظه كـ GitHub Secret وتوقيعه به.

### 4ب. الخطوط: خط الواجهة «Cairo» يُحمَّل من الإنترنت عند أول تشغيل
- السمة تستخدم `GoogleFonts.cairoTextTheme` → الخط يُنزَّل وقت التشغيل من خوادم Google؛ مع اتصالات اليمن البطيئة/المحجوبة سيعرض التطبيق خطاً بديلاً مؤقتاً أو دائماً.
- بالمقابل، خطوط `NotoNaskhArabic` و `DejaVuSans` (~1.7MB) مضمّنة في الـ APK كأصول لكنها **غير مسجلة في `fonts:` في pubspec** (تحققت من `FontManifest.json` داخل الـ APK: لا يحتويها) → وزن ميت غير مستخدم.
**الإصلاح**: تنزيل خط Cairo وضمّه إلى `pubspec.yaml` تحت `fonts:` واستخدام `fontFamily: 'Cairo'` مباشرة — يزيل اعتماد الشبكة ويحسّن الإقلاع.

### 5. غياب التحكم بالصلاحيات في بيانات الـ API
`quran-circles-api`: أي مستخدم مسجّل (حتى معلم) يستطيع:
- تعديل/تعطيل **أي** حلقة (`PUT/DELETE /api/halaqas/:id`) — لا فحص دور.
- تعديل/تعطيل **أي** طالب.
- إنشاء مستخدم جديد بدور `supervisor` عبر `POST /api/users` (الحقل `role` يؤخذ من جسم الطلب دون تحقق!).
- الاستثناء الوحيد المحمي: `/api/seed` و `/api/wipe`.

**الإصلاح**: إضافة فحوص `role` على مسارات الكتابة: الإنشاء/التعطيل للمشرف فقط، وتسجيل السجلات للمعلم على حلقته فقط.

### 6. نقطة `/api/data` تسمح لأي مستخدم بمسح واستبدال لقطة البيانات الكاملة
`PUT /auth/api/data` يتطلب توكن صالحاً فقط (أي دور) ويستبدل اللقطة بالكامل دون تحقق من الحجم أو الشكل. رغم أن التطبيق لا يستخدم هذه النقطة أصلاً (كود ميت على الخادم)، فهي خطر قائم.
**الإصلاح**: تقييدها بالمشرف + حد أقصى للحجم، أو حذفها إن لم تكن مستخدمة.

### 7. الـ API لا يدعم تعديل السجلات — وإعادة الإرسال تفشل بخطأ 500
- `daily_records` عليها قيد `UNIQUE(student_id, date_key)` لكن `POST /api/daily-records` عملية إدخال صريحة دون `ON CONFLICT` → إعادة حفظ نفس اليوم = خطأ 500.
- لا توجد مسارات `PUT/DELETE` للسجلات اليومية أو الخطط الأسبوعية → لا يمكن تصحيح خطأ إدخال أبداً.

### 8. إحصائيات لوحة التحكم مبنية على 500 سجل فقط
`GET /api/daily-records` بدون معاملات يرد بحد أقصى `LIMIT 500` (قيسته فعلياً: الرد = 500 بالضبط)، بينما البيانات الفعلية ~20 ألف سجل (296 طالباً × 12 أسبوعاً). لوحة التحكم، الرسوم البيانية، وعدّادات "حلقات لم تسجّل اليوم" كلها محسوبة من جزء عشوائي من البيانات.
**الإصلاح**: تجميع الإحصاءات في الخادم (`/api/stats`) بدل سحب السجلات كاملة، أو ترقيم صفحات.

### 9. الجلسة لا تُحفظ — دخول إجباري عند كل تشغيل، وفقدان التوكن عند أي إعادة تشغيل
- `ApiClient.authToken` متغير ساكن في الذاكرة فقط؛ `sessionProvider` حالة في الذاكرة.
- عند إغلاق التطبيق: تُفقد الجلسة → توجيه إلى `/login` دائماً.
- الأسوأ: إن فقد التوكن والبيانات تُجلب، كل الطلبات ترجع 401 دون توجيه واضح.
**الإصلاح**: حفظ التوكن والمستخدم في `SharedPreferences` واستعادتهما في `main()` مع تحقق `GET /api/me`.

---

## 🟡 النتائج المتوسطة (P2)

10. **لا يوجد تحديد معدل لتسجيل الدخول** (`/api/login`) — قابل لهجمات التخمين (يخففها PBKDF2 بـ 100 ألف تكرار، لكن يفضَّل إضافة إبطاء عبر Cloudflare WAF Rate Limiting).
11. **CORS مفتوح (`*`)** على كل الـ Workers مع السماح بـ `Authorization` — أي موقع يمكنه استدعاء الـ API بتوكن مسروق من متصفح الضحية.
12. **مقارنة التوقيع غير ثابتة الزمن** (`expected !== sig`) — نظرياً قابلة لهجوم توقيت؛ استبدلها بمقارنة البايتات بمنهج ثابت الزمن.
13. **زر «إعادة ضبط البيانات التجريبية» يظهر للجميع** ويستدعي `wipe()` ثم `seed()` اللذين يتطلبان توكن مشرف؛ إذا ضغطهما معلم → استثناء غير معالج وحوار تحميل لا يغلق.
14. **`gradle.properties` يطلب ذاكرة 8GB** (`-Xmx8G`) — يفشل البناء على الأجهزة محدودة الذاكرة (جهازي 1.9GB احتاج تخفيضاً يدوياً). اقتراح: `-Xmx2G` كافية لهذا المشروع.
15. **مطابقة `teacherIds.contains(id)` مطابقة نصية جزئية** (قد يتطابق معرّف قصير داخل معرّف آخر) — يجب `split(',')` ثم مقارنة دقيقة.
16. **سير عمل النشر `deploy-worker.yml` يستدعي `/api/seed` و `/api/users` بدون توكن** — بعد إضافة المصادقة أصبحت هذه الخطوات ترجع 401/403 والتقارير تُنشر بقيم `?`.
17. **`main.dart` يستدعي `seed()` عند كل إقلاع** بدون توكن → تفشل بصمت (مقبول لكنه هدر؛ الأفضل نقل الزرع لمشرف وبزر صريح).

## 🔵 النتائج المنخفضة (P3)

18. ملف `README.md` قديم: ما زال يصف «نسخة تجريبية محلية بالكامل بدون خادم» بينما التطبيق متصل سحابياً منذ عدة إصدارات.
19. `d1_database_id.txt` ملتزم في المستودع (معرفات غير حساسة لكن الأفضل في الإعدادات فقط).
20. `web/index.html`: وسما `description` مكررتان، إحداهما ما زالت «A new Flutter project».
21. `build-debug-report.md` تقرير قديم من حالة مكسورة (25 أغسطس) يخلط القارئ — يُفضل حذف التقارير الآلية الملتزمة.
22. ملاحظات `flutter analyze` السبع (استخدام `const`) — شكلية.

---

## 👍 نقاط القوة (تستحق الذكر)

- **معمارية نظيفة**: فصل واضح (UI → Providers → Repository interfaces → Data)، ما يسهل الإصلاحات أعلاه دون إعادة بناء.
- **نظام تحقق قوي في منطق التسميع**: منع الإغفال اليومي، قواعد الجمعة، نطاقات الآيات/الصفحات — كلها مُختبرة بـ 42 اختباراً ناجحاً.
- **بيانات المصحف دقيقة**: 114 سورة، 604 صفحات، مواضع بداية الصفحات مطابقة للمصحف (تحقق آلي).
- **تشفير كلمات المرور سليم**: PBKDF2-SHA256 بـ 100 ألف تكرار وملح عشوائي.
- **تجربة حجب اليمنيين مدروسة**: فكرة القنطرة `quran-center-bridge` مع روابط نسبية ومسارات داخلية (service bindings) حل ذكي لمشكلة حجب `pages.dev`/`workers.dev`.
- **دعم كامل للعربية وRTL والوضع الداكن.**

---

## 🛠️ خطة الإصلاح المقترحة (بالأولوية)

### المرحلة 1 — عاجلة (قبل أي توزيع)
1. تدوير `HMAC_SECRET` إلى سر عبر `wrangler secrets` وحذفه من الكود والمستودع.
2. ربط حسابات الدخول بجدول `users` (إنشاء متزامن أو ترحيل معرفات).
3. تمرير كتابة السجلات اليومية عبر الـ API + إضافة `PUT` مع `ON CONFLICT DO UPDATE`.

### المرحلة 2 — قبل النشر الواسع
4. مفتاح توقيع إصدار ثابت للـ APK.
5. فحوص صلاحيات على مسارات الكتابة في الـ API.
6. حفظ الجلسة محلياً واستعادتها عند الإقلاع.
7. نقطة `/api/stats` للوحة التحكم بدل حد الـ 500.

### المرحلة 3 — تحسينات
8. تحديد معدل الدخول + تضييق CORS على النطاقات المعروفة.
9. تحديث التوثيق والـ CI وتنظيف الملفات القديمة.

---

## 📎 ملحق: تفاصيل بيئة الفحص وتجربة البناء

**البيئة**: Linux x86_64 • ذاكرة 1.9GB • معالجان • لا يوجد swap • لا يوجد KVM (لا محاكي).

| الخطوة | النتيجة |
|---|---|
| Flutter 3.47.1 stable / Dart 3.13.1 | ثُبّت أثناء الفحص |
| `flutter pub get` | ✅ نجح |
| `flutter analyze` | ✅ 7 info فقط، 0 أخطاء |
| `flutter test` (42 اختباراً) | ✅ نجاح كامل في ~17 ثانية |
| `flutter build apk --release` محلياً | ⚠️ وصل إلى `assembleRelease` وعمل ~17 دقيقة ثم قُتل خادم Gradle لنفاد الذاكرة (بيئة 1.9GB بلا swap) |
| بناء CI (GitHub Actions) | ✅ نجاح مؤكد في 26 أغسطس — نزّلتُ المخرجات الفعلية (3 حزم: 28.9/27.1/30.5 ميجابايت) وفحصتها بـ `apksigner` و `aapt` |

**خلاصة البناء**: إعدادات المشروع (Gradle 8.14.2 + AGP 8.11.1 + Kotlin 2.2.20 + split-per-abi) **سليمة وتُنتج حزم صحيحة** بدليل نجاحها على CI وفحص الحزم الناتجة — فشل البناء المحلي سببه حصراً محدودية ذاكرة بيئة الفحص، مع ملاحظة أن `org.gradle.jvmargs=-Xmx8G` في المستودع مبالغ فيه ويسبب فشل البناء على أي جهاز أقل من 8GB (البند 14).

**ملاحظات على الحزم المفحوصة**:
- التوقيع: شهادة تصحيح `CN=Android Debug` (البند 4).
- الأذونات: `INTERNET` فقط ✅ — لا صلاحيات زائدة.
- `minSdk 24` (أندرويد 7+) — يغطي معظم الأجهزة، لكن بعض الأجهزة القديمة المنتشرة محلياً على أندرويد 5–6 لن تُغطّى.
- محتويات سليمة: مكتبات `libflutter.so`/`libapp.so`/`libsqlite3.so` لكل معمارية، الأصول والصور موجودة.

**حدود الفحص**: لم يمكن تشغيل التطبيق على محاكي (لا KVM) فاستُعيض عن ذلك بتحليل مسارات الكود بالكامل + فحص الـ APK الفعلي + اختبار الخوادم الحية. الاختبارات الأمنية نُفذت بطلبات قراءة فقط — **لم تُعدَّل أو تُحذف أي بيانات إنتاجية**.
